# Контекст сборки — корень монорепы (pnpm workspace).
# Отсекаем тяжёлое и лишнее; манифесты пакетов остаются, чтобы
# pnpm мог собрать граф воркспейса.
**/node_modules
**/dist
**/.output
**/.nuxt
**/.turbo
**/coverage
.git
**/.env
**/.env.*
!**/.env.example
*.sql
